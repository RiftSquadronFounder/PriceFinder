<Window x:Class="PriceFinder.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PriceFinder"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="1000">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition Width="0.2*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.1*"></RowDefinition>
            <RowDefinition Height="0.1*"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>


        <TextBox Margin="5" Name="PromptGUI"></TextBox>
        <Button Content="Поиск" Margin="5" FontFamily="consolas" FontSize="15" Grid.Column="1" Click="ToFind"></Button>
        <TextBlock Grid.Row="1" Name="TextLine111"></TextBlock>
        <Button Content="Выбор алгоритма" Margin="5" FontFamily="consolas" FontSize="15" Grid.Column="1" Grid.Row="1" Click="ChangeSearchMethod"></Button>


        <Border Background="AliceBlue" BorderThickness="1" Margin="5" Grid.Row="2" Grid.ColumnSpan="2">
            <ListBox Name="ShopItemsListGUI" HorizontalAlignment="Stretch">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <DockPanel HorizontalAlignment="Stretch">
                            <TextBlock Text="{Binding Path=Title}" DockPanel.Dock="Left"></TextBlock>
                            <TextBlock Text="{Binding Path=Price}" DockPanel.Dock="Right"></TextBlock>
                        </DockPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            
            
        </Border>
        
        
        

    </Grid>
</Window>
